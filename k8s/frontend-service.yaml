apiVersion: v1
kind: Service

metadata:
  name: mediassist-frontend-service

spec:
  selector:
    app: mediassist-frontend

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080

  type: NodePort