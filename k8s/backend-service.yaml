apiVersion: v1
kind: Service

metadata:
  name: mediassist-backend-service

spec:
  selector:
    app: mediassist-backend

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: ClusterIP